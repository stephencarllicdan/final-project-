<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar">


    <!-- Login Screen is the launcher -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Home/Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Register Activity -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <!-- Add Food/Entry Activity -->
        <activity
            android:name=".AddEntryActivity"
            android:exported="false" />

        <!-- View Entries Activity -->
        <activity
            android:name=".ViewEntriesActivity"
            android:exported="false" />

        <!-- Food Recommendation Activity -->
        <activity
            android:name=".FoodRecommendationActivity"
            android:exported="false" />

        <!-- Add Food Activity (if separate from AddEntry) -->
    </application>

</manifest>
